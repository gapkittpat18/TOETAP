TOETAP V2.1.1 — COMPARE UI FIX

Fixes:
- Removed Compare from top-right so it no longer conflicts with Profile.
- Compare is now a normal Shoes-page action.
- Replaced native browser shoe dropdowns with TOETAP light custom pickers.
- No more black native dropdown rows.
- Picker shows the full active shoe list and scrolls up to 300px instead of visually clipping options.
- Prevents selecting the same shoe on both sides.
- No SQL migration.
- No NFC/webhook/Latest Tap Wins changes.
